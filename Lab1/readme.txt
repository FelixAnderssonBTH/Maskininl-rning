Felix Andersson, Janine de Vries, DV2626

In order to run the program these installations are needed:

  >> pip install pandas scikit-learn imbalanced-learn numpy jupyter matplotlib

The code for the Assignment 1 is inside the jupiter program assignment1.ipynb. in order to run the program you just have to press "run all" in jupiter and the whole program will run

In order to visualize the csv file better for the analysis part in the report the inspection.py program was created. Each column will be visualized in a graph where the color represent number of ocurrancess. 
To run the code just run it in either the terminal or a code extension program like visual studio. The program will show a graph of the first column, in order to se the next one you need to exit the first.
